const menu=document.querySelector('.menu');const links=document.querySelector('.navlinks');menu?.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(item=>item.classList.remove('active'));button.classList.add('active');const kind=button.dataset.filter;document.querySelectorAll('.pub-list article').forEach(item=>item.hidden=kind!=='all'&&item.dataset.kind!==kind)}));document.getElementById('year').textContent=new Date().getFullYear();
